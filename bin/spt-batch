#!/usr/bin/env python3
"""
Spatelier Batch CLI shortcut.

Convenience script for batch operations.
"""

import sys
import os
from pathlib import Path

# Add the project root to Python path
project_root = Path(__file__).parent.parent
sys.path.insert(0, str(project_root))

# Import and run the batch CLI app
from cli.batch import app

if __name__ == "__main__":
    app()
